---
title: "LLM can Read Spectrogram: Encoder-free Speech-Language Modeling"
title_zh: LLM能读懂频谱图：无编码器的语音语言建模
authors: "Ruchao Fan, Yiming Wang, Yuxuan Hu, Bo Ren, Yufei Xia, Xiaofei Wang, Yao Qian, Jinyu Li"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10231v1"
tags: ["query:speech-model"]
score: 9.0
evidence: 无需编码器的语音语言模型，同时支持语音识别和语音合成
tldr: 传统语音大语言模型依赖预训练语音编码器提取声学特征，增加了架构复杂度。本文提出Mel-LLM，通过线性投影将Mel频谱图块直接输入LLM，让LLM自身学习语音-文本对齐，无需专用编码器。在ASR任务中，该方法在公开评测集和规模化实验中达到与编码器初始化模型接近的性能，数据有限时多模态预训练初始化至关重要。TTS任务通过next-token VAE初步验证了可行性。该工作展示了完全统一的免编码器架构在自回归语音-文本建模中的潜力，简化了模型结构。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 822, \"height\": 615, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 805, \"height\": 641, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 895, \"height\": 246, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1843, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1839, \"height\": 291, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1837, \"height\": 397, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10231v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 898, \"height\": 328, \"label\": \"Table\"}]"
motivation: 探究LLM能否不依赖专用语音编码器，直接学习读取Mel频谱图以实现语音-文本对齐。
method: 提出Mel-LLM，将Mel频谱图分块后线性投影直接输入LLM，通过自回归方式学习语音-文本映射。
result: ASR性能与编码器模型相当（仅轻微退化）；TTS采用next-token VAE实现初步可行结果。
conclusion: 免编码器架构可有效实现语音-文本统一建模，减少模型复杂度并保持竞争力。
---

## 摘要
最近的语音感知大语言模型（Speech-LLM）依赖预训练的语音编码器将音频转换为LLM可消费的语义丰富表示。相反，在这项工作中，我们探索：LLM能否直接学习读取梅尔频谱图，而无需专用的语音编码器？我们提出Mel-LLM，一种无编码器的Speech-LLM，通过线性投影将轻度预处理的梅尔频谱图块直接输入LLM，使LLM完全通过自身参数学习语音-文本对齐。我们在自动语音识别（ASR）和文本转语音（TTS）任务上进行了大量实验。对于ASR，我们在OpenASR排行榜公共集和生产级规模扩展实验上评估，表明无编码器解决方案仅与编码器初始化的对应方案相比有有限的性能下降，实现了有竞争力的性能。我们发现当数据有限时，从多模态检查点（Phi-4-MM）初始化对保持性能至关重要。我们还进行了消融研究，揭示了哪些LLM层与语音编码关系较小。对于TTS，我们展示了使用下一个token VAE方法的初步结果。虽然TTS性能尚未达到最优，但这些结果为自回归语音-文本建模的完全统一的无编码器架构建立了可行性。

## Abstract
Recent speech-aware large language models (Speech-LLMs) rely on a pre-trained speech encoder to convert audio into semantic-rich representations consumable by LLM. In this work, instead, we explore: can an LLM learn to read Mel spectrogram directly without a dedicated speech encoder? We propose Mel-LLM, an encoder-free Speech-LLM that feeds lightly pre-processed Mel spectrogram patches directly into the LLM through a linear projection, allowing the LLM to learn speech-text alignment purely through its own parameters. We conduct extensive experiments on both automatic speech recognition (ASR) and text-to-speech (TTS) tasks. For ASR, we evaluate on the OpenASR leaderboard public sets and production-level scaling experiments, demonstrating that the encoder-free solution achieves competitive performance with only limited degradation compared to encoder-initialized counterparts. We find that when data is limited, initialization from a multimodal checkpoint (Phi-4-MM) is crucial for maintaining performance. We also present ablation studies revealing which LLM layers are less relevant to speech encoding. For TTS, we show preliminary results with a next-token VAE approach. While TTS performance is not yet optimal, these results establish the feasibility of a fully unified encoder-free architecture for autoregressive speech-text modeling.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：当前主流的语音大语言模型（Speech-LLM）采用“预训练语音编码器 + 模态投影器 + 大语言模型”的三段式架构，依赖 Whisper、Conformer 等专用编码器提取声学表示。这种范式存在若干局限：
  - 编码器参数量大（如 Whisper-large >600M），增加计算开销；
  - 编码器的表示空间与 LLM 内部表示可能不匹配，形成信息瓶颈；
  - LLM 只能通过编码器的压缩表示间接理解语音，限制了信息流。
- **核心问题**：能否完全移除专用语音编码器，让 LLM 直接“阅读”原始的 Mel 频谱图，通过自身参数学习语音‑文本对齐？
- **整体含义**：如果能实现无编码器架构，可大幅简化模型结构、降低计算成本，并为真正统一的语音‑文本多模态模型铺平道路。受视觉领域 Fuyu、Tuna‑2 等无编码器工作的启发，论文尝试将该思想迁移到语音模态。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出 Mel‑LLM，将 Mel 频谱图经过轻量预处理（MVN 归一化 + 可选下采样卷积）后，直接通过**线性投影层**映射到 LLM 的嵌入空间，LLM 自身通过训练学会隐式地进行“语音编码”。
- **关键技术细节**：
  1. **ASR 输入路径**：
     - 提取 80 维 log‑Mel 特征 → 均值方差归一化（MVN）；
     - 可选卷积层进行时间下采样（降采样率 r=2 或 8，用于控制 token 率）；
     - 线性投影层（随机初始化）将特征映射到 LLM 隐藏维度；
     - 与文本 prompt 拼接后送入 LLM，自回归生成文本 token。
  2. **TTS 输出路径**（基于 MELLE 框架）：
     - LLM 在语音位置输出隐藏状态 → 通过 VAE 解码器（线性投影 → μ,σ → 重参数化 → 残差 MLP → PostNet）生成连续 Mel 帧；
     - 同时训练停等预测头，使用 L1+MSE、KL 散度、停等损失和谱通量损失联合优化；
     - 波形重建采用 HiFi‑GAN。
  3. **LLM 适配**：
     - 基座模型为 Phi‑4‑MultiModal（Phi‑4‑MM，32 层 Transformer，隐藏维度 3072），使用 LoRA 微调（秩 320，α=640），基座参数冻结。

### 3. 实验设计：数据集 / 场景、Benchmark、对比方法

- **ASR 实验**：
  - **训练数据**：公开英文语音语料 ~64k 小时（31M 条 utterance），包括 LibriSpeech、GigaSpeech、MLS English、SPGISpeech、CommonVoice、VoxPopuli、TED‑LIUM、AMI、Earnings‑22、FLEURS 等。另使用约 10× 规模的匿名内部数据用于生产级扩展实验。
  - **Benchmark**：OpenASR 排行榜的多个公共测试集（AMI、Earnings22、GigaSpeech、LS‑clean、LS‑other、SPGISpeech、TED‑LIUM、VoxPopuli），以词错误率（WER）为指标。
  - **对比方法**：
    - Whisper‑Large‑V3；
    - Phi‑4‑MM 直接使用（未微调）；
    - Phi‑4‑MM + FT（微调，含预训练编码器）；
    - 随机初始化编码器 + FT；
    - Mel‑LLM（Phi‑4‑MM 初始化，无编码器）；
    - Mel‑LLM（随机初始化，无编码器）。
- **TTS 实验**：
  - **训练数据**：Libriheavy 50k 小时英文数据。
  - **评测**：零样本合成，测试集为 LibriSpeech test‑clean，评估指标为 WER（Whisper‑large‑v3 识别）和 UTMOS 感知质量。
- **生产级扩展实验**：在匿名内部测试集（Call Center、Conversation、Dictation）上对比编码器初始化和无编码器模型在有限数据与 10× 数据下的差距。

### 4. 资源与算力

- 文中明确说明：使用 **16 块 NVIDIA H100 GPU**，采用 DeepSpeed ZeRO Stage‑1 进行分布式训练。
- 优化器：AdamW，峰值学习率 1e‑4，线性预热‑衰减（9000 步预热），梯度裁剪 1.0，有效 batch size 512。
- 训练轮次：公开数据迭代 3 轮（更多无增益）；内部数据 1 轮；TTS 数据 5 轮。
- 未明确给出完整训练时长，但提供了训练速度对比：12.5Hz token 率时 Mel‑LLM 比编码器基线加速 1.57×。

### 5. 实验数量与充分性

- **实验组数**：约 5 组主要实验表格（表 1‑5），加上若干文本描述的分析。
  - 表1：OpenASR 8 个测试集下 6 种系统对比；
  - 表2：生产规模扩展实验（3 个域，两种数据量级）；
  - 表3：下采样率消融（5 种 token 率，8 个测试集）；
  - 表4：层冻结消融（5 种冻结模式，8 个测试集）；
  - 表5：TTS 初步结果（多种配置，4 组有效结果）。
- **充分性评价**：
  - ASR 方面覆盖了多种领域、数据集和基线对比（包括有/无编码器、不同初始化），消融实验充足，揭示了数据规模、下采样率、层敏感性的影响，结论可靠。
  - TTS 方面仅提供了初步结果，未与其他 SOTA TTS 系统对比，实验数量较少，属于可行性验证。
  - 整体而言，ASR 实验设计充分、客观，对比公平（类似训练配置、LoRA 相同秩等）。TTS 部分尚不充分。
- **偏差风险**：实验仅基于 Phi‑4‑MM 基座，未扩展到其他 LLM；公开数据主要为英文，多语言未验证；TTS 评测仅用 WER 和 UTMOS，缺乏主观听感测试。

### 6. 论文的主要结论与发现

1. **ASR 性能**：无编码器 Mel‑LLM（Phi‑4‑MM 初始化）在 OpenASR 上平均 WER 7.12%，仅比随机编码器基线（6.97%）差 0.15%，且优于完全随机初始化的 Mel‑LLM（7.44%）。说明多模态预训练初始化在数据有限时至关重要。
2. **数据规模是关键**：当训练数据从 64k 小时扩展到 10× 内部数据时，无编码器与编码器模型之间的相对差距从 11.3% 缩小到 3.8%，证明足够数据可弥补编码器的缺失。
3. **LLM 下层负责隐式语音编码**：层冻结消融表明，冻结最后 8 层（24‑31）几乎不影响 ASR 性能，而冻结更多下层则性能显著下降。揭示 LLM 的低层（0‑23）需要学习声学特征处理，高层已具备高级语义能力。
4. **下采样率权衡**：12.5Hz token 率取得最佳质量‑速度权衡（训练加速 1.57×），更高 token 率略有更好 WER 但速度大幅下降。
5. **TTS 的初步可行性**：基于 next‑token VAE 的 TTS 可输出可识别语音，但需要 Phi‑4‑MM 初始化和大 dropout（默认 0.5）；当前性能尚未达到最优。

### 7. 优点（方法或实验设计亮点）

- **创新性**：首次在语音‑LLM 领域系统验证无编码器架构的可行性，挑战了“编码器必不可少”的传统假设。
- **实验设计严谨**：
  - 公平对比：与编码器基线保持相同 LoRA 配置、训练数据、评测流程；
  - 多维度消融：涵盖初始化策略、数据规模、下采样率、层冻结，提供了深入的分析；
  - 生产级扩展验证：使用大规模内部数据证实数据扩展可缩小差距。
- **可解释性贡献**：通过层冻结实验揭示了 LLM 内部的职能分工——下层做声学编码，上层做语言建模，支持了 NLP 中“底层局部特征、顶层语义”的通用认知。
- **架构简化与效率**：无编码器 ASR 训练速度提升 1.57×，且参数更少，易于部署。

### 8. 不足与局限

- **尚未实现统一训练**：ASR 和 TTS 分开训练，未探索联合训练或语音‑only 预训练，距离真正的统一语音‑文本模型还有差距。
- **TTS 结果初步**：TTS 性能不理想（WER 11.03%‑18.07%，UTMOS 3.10‑3.29），缺乏与 MELLE、MELA‑TTS 等开源 TTS 的直接对比；未提供主观听测或 MOS 评分，结果说服力有限。
- **依赖强初始化**：Phi‑4‑MM 多模态预训练是数据有限时保持性能的关键；若完全随机初始化，ASR 退化 0.32%，TTS 甚至无法输出可听语音。这限制了该方法在无多模态基座场景下的应用。
- **单基座、单语言**：仅基于 Phi‑4‑MM（32 层），未验证其他规模或类型的 LLM（如 LLaMA、Qwen）；仅使用英文数据，多语言性能未知。
- **缺失端到端生成对比**：未与直接生成波形的模型（如 WavFlow）对比，也未评估 TTS 合成的自然度、韵律等维度。
- **潜在过拟合风险**：公开数据仅迭代 3 轮，内部数据 1 轮，TTS 数据 5 轮；对于更大模型或更复杂任务可能需要更多轮次或正则化。
- **消融分析仅基于 ASR**：层的重要性仅在 ASR 任务上验证，未在 TTS 或联合任务上确认。

（完）
