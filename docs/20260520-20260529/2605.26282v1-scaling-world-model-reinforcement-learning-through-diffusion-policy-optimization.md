---
title: Scaling World-Model Reinforcement Learning Through Diffusion Policy Optimization
title_zh: 通过扩散策略优化扩展世界模型强化学习
authors: "Xiaoyuan Cheng, Wenxuan Yuan, Zhancun Mu, Yuanzhao Zhang, Yiming Yang, Hai Wang, Zhuo Sun, Che Liu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26282v1"
tags: ["query:wm-vla-sa"]
score: 9.0
evidence: 世界模型强化学习
tldr: 基于世界模型的强化学习面临模型偏差、误差累积及搜索与价值学习间的结构不对齐问题。本文提出模型基扩散策略优化（MBDPO），通过扩散策略表示统一搜索与策略优化，从数据集提取隐式能量函数锚定策略，并细化得分场进行优化。在多任务离线预训练、在线学习及离线到在线微调中验证有效性，离线大规模预训练显示随模型容量增加性能单调提升。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26282v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 1099, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 383, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-004.webp\", \"caption\": \"\", \"page\": 3, \"index\": 4, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-005.webp\", \"caption\": \"\", \"page\": 3, \"index\": 5, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 5192, \"height\": 1650}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 4050, \"height\": 1399}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-008.webp\", \"caption\": \"\", \"page\": 21, \"index\": 8, \"width\": 1659, \"height\": 948}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-009.webp\", \"caption\": \"\", \"page\": 21, \"index\": 9, \"width\": 1644, \"height\": 957}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-010.webp\", \"caption\": \"\", \"page\": 22, \"index\": 10, \"width\": 1600, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-011.webp\", \"caption\": \"\", \"page\": 22, \"index\": 11, \"width\": 1991, \"height\": 789}]"
motivation: 现有世界模型方法中搜索与价值学习存在结构不对齐，导致训练不一致和次优学习。
method: 将策略优化重构为在潜世界模型中对搜索轨迹的扩散过程，利用数据集提取隐式能量函数并细化得分场。
result: 在多任务离线预训练、在线学习及离线到在线微调中均优于基线，离线大规模预训练性能随模型容量单调提升。
conclusion: MBDPO通过扩散策略统一搜索与优化，有效缓解不对齐问题，为世界模型可扩展策略学习提供新框架。
---

## 摘要
基于模型的强化学习（RL）可以通过使用世界模型在规模上得到有效支持。然而在实践中，扩展此类方法仍然受到根本性限制。一个公认的挑战是模型偏差和误差累积，这会降低长期预测的质量。除了这些问题，我们识别出一个更关键但尚未充分探索的瓶颈：现有世界模型方法中搜索与价值学习之间的结构性错位。具体而言，策略改进通常依赖于由独立非搜索策略引起的价值函数，导致训练不一致并最终产生次优学习。为解决这一限制，我们提出世界模型中的基于模型的扩散策略优化（MBDPO），该框架通过扩散策略表示统一了搜索和策略优化，从而释放世界模型在可扩展策略学习中的潜力。我们不在学到的世界模型上构建显式规划器，而是将策略优化重新表述为潜在世界模型中搜索轨迹上的扩散过程。在此视角下，我们从收集的数据集中提取隐式能量函数来锚定策略，使MBDPO能够细化用于策略优化的分数场，同时缓解错位问题。我们在多种设置下评估MBDPO，包括多任务离线预训练、在线学习以及离线到在线的微调。在离线场景中，我们进一步通过在大规模数据集上预训练来研究其扩展行为，观察到随着模型容量的增加，性能持续单调提升。

## Abstract
Model-based reinforcement learning (RL) can be effectively supported at scale through the use of world models. However, in practice, scaling such approaches remains fundamentally limited. A commonly recognized challenge is model bias and error compounding, which degrade long-horizon predictions. Beyond these issues, we identify a more critical yet underexplored bottleneck: a structural misalignment between search and value learning in existing world model approaches. In particular, policy improvement often relies on value functions induced by a separate, non-search policy, resulting in training inconsistency and ultimately suboptimal learning. To address this limitation, we propose Model-Based Diffusion Policy Optimization (MBDPO) in world models, a framework that unifies search and policy optimization through diffusion policy representations, thereby unlocking the potential of world models for scalable policy learning. Instead of constructing an explicit planner over a learned world model, we reformulate policy optimization as a diffusion process over searched trajectories in latent world models. In this view, we extract an implicit energy function from the collected dataset that anchors the policy, enabling MBDPO to refine the score field for policy optimization while mitigating misalignment. We evaluate MBDPO across a wide range of settings, including multi-task offline pretraining, online learning, and offline-to-online fine-tuning. In the offline regime, we further investigate its scaling behavior by pretraining on large-scale datasets, observing consistent and monotonic performance gains with increasing model capacity.

---

## 论文详细总结（自动生成）

# 中文论文总结

## 1. 核心问题与整体含义（研究动机和背景）

**问题**：基于世界模型的强化学习在扩展时面临根本性限制。现有方法存在两个主要问题：

- **模型偏差与误差累积**：长期预测中微小的一步误差会放大，导致状态估计不可靠。
- **更关键的瓶颈：搜索与价值学习之间的结构性错位** —— 策略改进依赖非搜索策略（behavior policy）训练的价值函数，而策略执行（如MPPI搜索）使用搜索轨迹，导致分布不一致。价值函数在分布外状态-动作对上被查询时不可靠（通常过度乐观），搜索利用这些错误，造成系统性高估偏差，损害性能和可扩展性。

**整体含义**：本文旨在通过扩散策略表示统一搜索和策略优化，从而解放世界模型在可扩展策略学习中的潜力。提出 **MBDPO（Model-Based Diffusion Policy Optimization）** ，将策略优化视为潜世界模型中搜索轨迹上的扩散过程，利用隐式能量函数锚定策略，缓解错位问题。

## 2. 方法论：核心思想、技术细节、算法流程

### 核心思想
- 将策略优化重新表述为扩散过程，通过世界模型提供的想象轨迹进行得分匹配。
- 利用从数据集学到的隐式能量函数 \(E(z,a)\) 近似行为策略密度，用于KL散度约束（Trust Region）。
- 得分函数 \(\phi(z_t, a^\tau_{t:t+H}, \tau)\) 通过世界模型评估的加权蒙特卡洛期望计算，从而把生成式去噪过程转变为基于模型的策略优化。

### 关键技术细节
- **潜世界模型**：编码器 \(E\)、潜动力学 \(F\)、潜奖励 \(R\)，全部在潜空间 \(z\) 中操作。
- **扩散策略**：采用DDPM框架，反向扩散核由得分函数引导：
  \[
  a^{\tau-1}_{t:t+H} = \frac{1}{\sqrt{\alpha_\tau}} \bigl(a^\tau_{t:t+H} + (1-\alpha_\tau)\phi(z_t, a^\tau_{t:t+H}, \tau)\bigr) + \sigma(\tau)\epsilon
  \]
- **得分函数计算**（公式10-15）：
  \[
  \phi(z_t, a^\tau_{t:t+H}, \tau) \approx -\frac{a^\tau_{t:t+H}}{1-\bar\alpha_\tau} + \frac{\sqrt{\bar\alpha_\tau}}{1-\bar\alpha_\tau} \sum_{i=1}^T w_i \cdot a^{0|\tau,(i)}_{t:t+H}
  \]
  其中权重 \(w_i\) 基于能量正则化累积回报 \(\tilde{G}\) 的 softmax。
- **隐式能量函数**：通过对比学习（公式9）从缓冲区数据学习，近似 \(-E(z,a) \approx \log \beta(a|z) + C(z)\)，用于正则化奖励 \(\tilde{R} = R - \eta \gamma^{h-t} E\)。
- **优化目标**：联合世界模型损失（公式14，包括动力学、奖励、Q函数、能量损失）和得分网络L2损失（公式16）。

### 算法流程（Algorithm 1）
1. **热身**：随机动作填充缓冲区，训练初始世界模型。
2. **主循环**（在线学习）：
   - **与环境交互**：通过扩散过程采样动作执行。
   - **世界模型更新**：采样批数据，最小化联合损失。
   - **扩散策略优化**：利用世界模型想象轨迹估计得分，更新得分网络 \(\hat{\phi}\)。
3. **离线预训练**：跳过交互步骤，仅从固定数据集学习。
4. **离线到在线微调**：加载预训练参数后在线交互。

## 3. 实验设计

### 数据集 / 场景
- **DMControl**（39个任务）：连续控制，包括视觉RL（10个图像任务）。
- **MetaWorld**（50个任务）：多目标操作任务。
- **ManiSkill2**（5个任务）：物体操纵，含YCB物体集。
- **MyoSuite**（10个任务）：生理学精确的肌肉骨骼控制。
- **Locomotion**（7个任务）：高维运动控制（Dog、Humanoid）。
- 总计 **121个任务**，涵盖状态/动作高维、稀疏奖励、多物体操纵等挑战。

### Benchmark
- 多任务离线预训练（80/30任务集）、在线从零训练、离线到在线微调（70任务预训练→10个未见任务微调）。

### 对比方法
- **SAC**（模型无关基线）
- **DreamerV3**（世界模型方法）
- **TD-MPC** 和 **TD-MPC2**（当前最先进的模型-模型结合方法）
- **MBDPO**（本文方法）

## 4. 资源与算力

- **在线训练**：单张 NVIDIA A800-80GB GPU，每环境步耗时毫秒级（Table 1a），与TD-MPC2相当或略快。
- **离线多任务训练**：单张 NVIDIA H200 GPU，10M训练步数耗时从34.8小时（1.7M参数/30任务）到206.8小时（340M/80任务）（Table 1b）。
- 未提及多卡分布式训练或总GPU小时数。

## 5. 实验数量与充分性

- **实验数量**：非常充分。
  - 4个基准共121个任务，每个任务3个随机种子。
  - 多任务离线预训练：5种模型尺寸（1.7M–340M参数量），2种规模数据集。
  - 消融实验：正则化因子 \(\eta\)（图8）、扩散去噪步数 \(N\)（图10）、蒙特卡洛样本数 \(T\)（图9）。
  - 可视化：潜轨迹（单/多episode）对比TD-MPC2。
  - 交叉TD误差和动作漂移度量（图3）。
- **公平性**：超参数严格遵循TD-MPC框架；与基线在同一条件下比较；使用标准化分数聚合。
- **客观性**：多种子取均值、提供误差范围；消融实验覆盖关键因素。

## 6. 主要结论与发现

- MBDPO在所有121个任务上一致优于或匹敌最强基线（SAC, DreamerV3, TD-MPC, TD-MPC2）。
- **单调缩放行为**：在多任务离线预训练中，归一化分数随模型参数（1.7M→340M）单调提升（图6），而TD-MPC2提升较慢且在小模型时性能更差。
- **缓解错位问题**：交叉TD误差显著降低（图3左），动作漂移更小（图3右），验证了隐式能量函数约束的有效性。
- **潜轨迹物理一致性**：MBDPO生成的潜轨迹呈现清晰闭环（周期性任务）或平滑目标导向路径（操作任务），而TD-MPC2轨迹杂乱（图11）。
- **样本效率**：离线到在线微调中，预训练模型仅用40k交互即可超过从头训练（图7）。

## 7. 优点

- **理论创新**：从价值迭代和策略改进两个角度严格证明了搜索-价值错位问题（Problem 1 & 2），并证明得分匹配等价于KL约束下的最优Gibbs策略（Theorem 1）。
- **统一框架**：将搜索与策略优化统一为扩散过程，解决了现有方法中启发式搜索（MPPI）的不稳定性。
- **实验充分**：大规模多任务、多种设置、丰富消融、可视化分析，结果可靠。
- **计算高效**：在线训练时间与TD-MPC2相当，离线训练开销合理。
- **实用价值**：离线→在线微调方案极具应用潜力，通用预训练可快速适应新任务。

## 8. 不足与局限

- **参数规模受限**：最大模型为340M参数，未探索极致大规模（如B级以上），尚未验证极限扩展行为。
- **评估范围有限**：仅在仿真环境中测试，未在真实机器人上部署，泛化到真实世界的鲁棒性未验证。
- **依赖性**：依赖于带标签的任务嵌入和离线数据集质量；若预训练数据分布偏差严重（如“Hopper Hop”任务），性能可能受阻。
- **未利用基础模型**：未集成预训练视觉/多模态编码器，可能限制了表征质量。
- **理论假设**：理论推导假设连续状态空间和短视域下状态变化较小，严格适用性未必覆盖所有场景。
- **消融分析**：对能量函数损失权重\(\eta\)的取值仅在0–5范围，极端值（如>10）的影响未分析。

（完）
